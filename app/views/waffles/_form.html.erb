<h1><%= headerText %></h1>

<%= form_for @waffle do |w| %>

    <% if @waffle.errors.any? %>
        <div id="error_explanation">
            <h2>
                <%= pluralize(@waffle.errors.count, "error") %> prohibited
                this article from being saved:
            </h2>
            <ul>
                <% @waffle.errors.full_messages.each do |msg| %>
                    <li><%= msg %></li>
                <% end %>
            </ul>
        </div>
    <% end %>

    <p>
        <%= w.label :name, 'Nazwa' %><br>
        <%= w.text_field :name %>
    </p>

    <p>
        <%= w.label :price, 'Cena' %><br>
        <%= w.text_field :price %>
    </p>

    <p>
        <%= w.submit submitText %>
    </p>

<% end %>

<%= link_to 'Wróć', waffles_path %>